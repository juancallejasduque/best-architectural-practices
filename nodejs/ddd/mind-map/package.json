{
  "name": "mind-map",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node ./dist/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "DDD samples - Mind map",
  "devDependencies": {
    "@types/node": "^24.7.1",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  }
}
